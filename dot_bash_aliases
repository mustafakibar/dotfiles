# ~/.bash_aliases — backwards-compatibility shim.
# The real content lives in ~/.config/shell/aliases.sh, shared by bash and zsh.
[ -r "$HOME/.config/shell/aliases.sh" ] && . "$HOME/.config/shell/aliases.sh"
