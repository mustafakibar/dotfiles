# ~/.bash_aliases — geriye dönük uyumluluk kabuğu.
# Gerçek içerik ~/.config/shell/aliases.sh içinde (bash ve zsh ortak).
[ -r "$HOME/.config/shell/aliases.sh" ] && . "$HOME/.config/shell/aliases.sh"
